micronaut.application.name=events-producer
micronaut.server.port=8081

kafka.enabled=true
kafka.bootstrap.servers=localhost:9092

# Auto-generate random test events (accountIds, userIds, opType pools) every minute
simulator.auto-generate.enabled=false
simulator.auto-generate.events-per-minute=1000